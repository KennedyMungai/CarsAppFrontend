import React from "react";

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer className="flex items-center justify-around w-full">Footer</footer>
  );
};

export default Footer;
