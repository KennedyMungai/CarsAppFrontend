import React from "react";

type Props = {};

const Header = (props: Props) => {
  return (
    <nav className="flex items-center justify-around w-full p-20">Header</nav>
  );
};

export default Header;
